# コバルト進捗 — 5鉱物計画 A0

## 2026-09-15 更新

認証APIの限定12クエリ取得・オフライン再生を完了（158観測行、4空応答）。
USGSに加えBGS・DRC鉱山省・NRCan・企業原報告と比較し、国連のHS版間対応も検証した。
詳細・出典・未解決事項は [一次資料の比較・差異台帳](cobalt-source-audit-2026-09-15.md)。
`source-review.json` に原本参照付き60件とHS対応18行を保存。
カナダ2024の新版値は国内統計と整合する一方、DRCの年次表示・過去年改訂、
カナダ2023・DRC/インドネシア/トルコ2024の資料間差、DRC貿易欠測は残る。

A0の契約上限確認は未確定（試行成功から推定しない）。A2/A3の限定比較まで進行。
A1の全期間取得と段階別公開は未完了。公開数値・HHIの変更なし。
以下は初回着手時の履歴で、取得・レビュー「未実施」の記述は今回更新前の状態を指す。

## 2026-09-14 着手時点の記録

2026-09-14。着手済み、A0の診断・棚卸しを保存。A0全完了ではない。
作業ブランチ: `codex/cobalt-a0`。基準main: `4f33b72ef40dd4936a32b4a530e4b972b75d83ed`。
旧Task A（リチウム段階タブ）とは別。A1以降・公開判定は未着手。

## 既存成果と並行作業

- 最新remote mainは基準と一致。17:28 UTCの公開APIと認証CLIでopen PRは0。
  未公開の他担当作業までは確認できないため、本取得前に再確認する。
- Git管理下の `data/raw` に cobalt/260500/810520 のJSON一致なし。
  `data/manual` にコバルト専用入力なし。共通 `world_production.csv` はコメントとヘッダのみ。
  `critical-minerals/data/minerals/cobalt.json` は存在しない。
  catalogとHTMLは存在するが、既存のコバルト数値系列があるという前提は成立しなかった。
- リチウム・天然黒鉛の公開JSON、index、catalog、HTMLなど37ファイルのSHA256を
  `data/review/cobalt/baseline.json` に保存。元の作業場所の未コミットHTML等は変更していない。
- GitHub CLI単独では未認証だったが、既存 `.cache/github.ps1` による認証は利用可能。
  `COMTRADE_API_KEY` の登録名を確認。キー値の取得・表示はしていない。

## 取得実績

`data/review/cobalt/a0-probe.json` がクエリ／試行台帳。
原本文・URL・クエリ・取得日時・SHA256を同ディレクトリ内 `data/raw` に保存。
全件 `publishable: false`。

| 2024年・World相手・年次HS | 最終応答 | 意味 |
|---|---|---|
| DRC(180)輸出・260500 | HTTP200、count=0 | 空応答。輸出ゼロと解釈しない |
| 中国(156)輸入・260500 | HTTP200、count=1 | preview診断、分類H6。採用・正規化は未実施 |
| DRC(180)輸出・810520 | HTTP200、count=0 | 空応答。ミラーで自動置換しない |
| 中国(156)輸入・810520 | HTTP200、count=1 | preview診断、分類H6。中国のみを世界ミラーと呼ばない |

初回4リクエスト中3件が429、Retry-Afterは2秒。3秒間隔で各1回再試行し200。
初回失敗3件も原本保持。計7貿易応答、USGS PDF 2件、GitHub PR一覧1件を保存。
上限500到達は観測していないが、認証APIの上限や全世界取得可能性を証明しない。
公式の[API案内](https://uncomtrade.org/docs/un-comtrade-api/)はpreviewと認証APIを区別。
[契約案内](https://uncomtrade.org/docs/subscriptions/)の100K/回・500回/日は一般案内であり、
このキーの契約・実効上限は未確認。

[USGSコバルト資料一覧](https://www.usgs.gov/centers/national-minerals-information-center/cobalt-statistics-and-information)
から参照できるMCS 2025/2026のコバルトPDFを保存。本文先頭のPDF識別と原本ハッシュを確認。
表の画像確認、数値抽出、推計・単位・脚注のレビューは未実施。採用スナップショットではない。

## 対象範囲と残る確認

| 段階 | 対象 | 単位・範囲上の扱い |
|---|---|---|
| 鉱石・精鉱 | 現行260500 | 貿易netWgtはkg、qtyはunitを併記。含有Co量へ換算しない |
| 中間品・未加工金属・粉 | 現行810520 | 単一の精製段階とは呼べない。鉱石重量と合算しない |
| 酸化物・水酸化物 | 追加検討282200 | 現行headline外。追加効果を調査してから範囲変更を判断 |
| 鉱山生産 | USGSコバルト表 | 含有量・単位・推計・世界計は原表レビューで確定 |

UNSDの[HS2017 810520](https://unstats.un.org/unsd/classifications/Econ/Detail/EN/2089/810520)と
[HS2017 282200](https://unstats.un.org/unsd/classifications/Econ/Detail/EN/2089/282200)で名称を確認。
HS2012との対応リンクも確認。HS2022/H6および2017〜2024の各報告版・換算系列の対応確認は残る。
USDは金額系列として保持し、重量・鉱山量と別系列にする。既存headline2コードは変更なし。
基本期間2017〜2024、2025は別枠。国・地域登録と指標への採否も別に管理する。

## A0残作業とA1実行計画

1. Actionsにコバルト専用の小規模認証プローブを追加し、登録済みシークレットを環境注入。
   本番更新ワークフローの転用やキーのローカル持ち出しは不要。
   実際のcount・契約上限・429/5xx・次元・H6対応を確認するまでA0完了にしない。
2. 既存2コード×8年×X/Mの32クエリを出発点とし、Worldと二国間を明示して分離。
   全reporter/partnerのワイルドカード仕様を確認し、上限到達時は報告国単位で非重複分割。
   分割一覧・期待集合・複合キー重複を検証し、count不整合・空応答を成功集合から区別。
3. 成功結果を再利用。Retry-Afterを守る上限付き再試行、失敗台帳、再開処理を本取得側に実装。
   preview診断スクリプトは本取得コレクタとして使用しない。
4. USGS過去版目録を拡張。B担当との取得重複を再確認し、版ごとの原表と改訂差を保持する。
   A2の画像レビュー前に数値を公開しない。
5. 282200追加は採否未決。現行範囲との差分・対象年の分類対応をまとめて判断する。

## 再現と検証

作業ブランチのルートで `python -m pipeline.cobalt_probe`。
通信なしで原本10応答と公開基準37ファイルのハッシュ一致を確認する。
`--fetch` は新規取得専用で既存台帳の上書きを拒否。
`--retry-failed` は最新試行が429/5xxのクエリだけを各1回再試行し、履歴を追記する。

公開データ・指標は変更なし。HTML再レンダリングのGit差分なし（Windows改行はLFへ正規化）。
既存ユニットテストは225 passed（Python 3.14、174.08秒）。初回は一時ディレクトリの
親が存在せず73件がsetup errorとなったため、親を作成して再実行し全件通過。
テスト自体もHTMLを生成するため、終了後にLF正規化と基準ハッシュ照合を再実施。

次に触るファイル: コバルト認証プローブ、専用Actions workflow、クエリ計画、範囲対応表。
この保存は全コバルトデータの検証完了・公開完了・mainへのマージを意味しない。
